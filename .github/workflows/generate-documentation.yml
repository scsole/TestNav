name: Generate Docs

on:
  push:
    branches: master

jobs:
  dokka:
    runs-on: ubuntu-latest
    steps:
      - uses: everlytic/branch-merge@1.0.2
        with:
          source_ref: master
          target_branch: gh-pages
      - uses: actions/checkout@v2
        with:
          ref: gh-pages
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Generate docs
        run: |
          rm -r dokka
          ./gradlew dokka
      - name: Push changes
        run: |
          git config --global user.name 'Github Actions'
          git config --global user.email 'github-actions@github.com'
          git add -f dokka
          git diff-index --quiet HEAD || git commit -m "[Bot] update docs"
          git push
